<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Readings - <%= device.name %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

</head>

<body>
    <div class="container mt-4">
        <h1 class="mb-4">Dữ liệu cảm biến của <%= device.name %> (<%= device.device_uid %>)</h1>

        <!-- Khung cảnh báo -->
        <div id="alert-box"></div>

        <!-- Bảng dữ liệu -->
        <div class="card mb-4">
            <div class="card-header bg-dark text-white">📋 Bảng dữ liệu</div>
            <div class="card-body p-0">
                <table class="table table-striped table-hover mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th>Thời gian</th>
                            <th>Nhiệt độ (°C)</th>
                            <th>Độ ẩm (%)</th>
                            <th>Ánh sáng</th>
                            <th>Tiếng ồn</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% readings.forEach(r=> { %>
                            <tr>
                                <td>
                                    <%= r.ts %>
                                </td>
                                <td>
                                    <%= r.temperature %>
                                </td>
                                <td>
                                    <%= r.humidity %>
                                </td>
                                <td>
                                    <%= r.light %>
                                </td>
                                <td>
                                    <%= r.noise %>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="mb-3">
            <label for="rangeSelect" class="form-label">📅 Chọn khoảng thời gian:</label>
            <select id="rangeSelect" class="form-select w-auto d-inline-block">
                <option value="24h">24 giờ gần nhất</option>
                <option value="7d">7 ngày gần nhất</option>
                <option value="custom">Tùy chỉnh...</option>
            </select>

            <input type="date" id="startDate" class="form-control d-inline-block w-auto" style="display:none;">
            <input type="date" id="endDate" class="form-control d-inline-block w-auto" style="display:none;">
            <button id="applyRange" class="btn btn-primary" style="display:none;">Áp dụng</button>
        </div>

        <!-- Biểu đồ -->
        <div class="card">
            <div class="card-header bg-dark text-white">📈 Biểu đồ</div>
            <div class="card-body">
                <canvas id="chart" height="300"></canvas>
            </div>
        </div>

        <p class="mt-3"><a class="btn btn-secondary" href="/dashboard">⬅ Quay lại Dashboard</a></p>
    </div>

    <div id="chart-data" data-labels='<%- JSON.stringify(readings.map(r => r.ts)) %>'
        data-temp='<%- JSON.stringify(readings.map(r => r.temperature)) %>'
        data-hum='<%- JSON.stringify(readings.map(r => r.humidity)) %>' data-deviceid='<%= device.id %>'>
    </div>



    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/luxon@3"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-luxon@1"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/readingsPageScript.js"></script>

</body>

</html>