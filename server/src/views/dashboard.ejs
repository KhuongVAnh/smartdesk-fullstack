<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smartdesk Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

</head>

<body>
    <div class="container mt-4">
        <h1 class="mb-4">📊 Danh sách thiết bị</h1>
        <a class="btn btn-primary mb-3" href="/devices/new">➕ Thêm thiết bị</a>

        <table class="table table-striped table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>UID</th>
                    <th>Tên</th>
                    <th>Token</th>
                    <th>Last Seen</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <tbody>
                <% devices.forEach(d=> { %>
                    <tr>
                        <td>
                            <a href="/readings/<%= d.id %>">
                                <%= d.id %>
                            </a>
                        </td>
                        <td>
                            <%= d.device_uid %>
                        </td>
                        <td>
                            <%= d.name %>
                        </td>
                        <td>
                            <%= d.token %>
                        </td>
                        <td>
                            <%= d.last_seen %>
                        </td>
                        <td>
                            <a href="/devices/edit/<%= d.id %>" class="btn btn-sm btn-warning">✏️ Sửa</a>
                            <a href="/devices/delete/<%= d.id %>" class="btn btn-sm btn-danger"
                                onclick="return confirm('Bạn có chắc muốn xóa?');">🗑️ Xóa</a>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    </div>
</body>

</html>